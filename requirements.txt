# Runtime deps used by bng_simulator / bng_controller
beamngpy
PyYAML
numpy
tqdm

# Optional: schema-based rosbag2 (MCAP) decoding without ROS msg packages
rosbags

# Build-time deps when colcon uses a venv python
empy
catkin_pkg
lark-parser